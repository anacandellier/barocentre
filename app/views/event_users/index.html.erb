<ul>
  <% @eventusers.each do |element| %>
    <li><%= element.user.email %></li>
  <% end %>
</ul>

<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"> MAP
</div>
